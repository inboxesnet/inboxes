{$DOMAIN} {
	# API and webhook routes → backend
	handle /api/* {
		reverse_proxy backend:8080
	}

	# WebSocket upgrade
	handle /api/ws {
		reverse_proxy backend:8080
	}

	# Everything else → Next.js frontend
	handle {
		reverse_proxy frontend:3000
	}
}
