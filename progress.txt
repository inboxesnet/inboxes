# Progress Log

## Learnings
- create-next-app interactive prompts don't work well in non-interactive shells; manually create files instead
- shadcn/ui needs: class-variance-authority, clsx, tailwind-merge, lucide-react, tailwindcss-animate
- shadcn/ui components.json + lib/utils.ts with cn() helper is the foundation
- tailwind.config.ts needs CSS variable-based colors and tailwindcss-animate plugin
- globals.css needs @layer base with CSS variables for light/dark themes
- Project name must be lowercase in package.json for npm compatibility
- Prisma 7 removes `url` from datasource block; downgraded to Prisma 5 for traditional config
- Use `DATABASE_URL="..." npx prisma validate` to validate schema without .env file
- Use `prisma migrate diff --from-empty --to-schema-datamodel` to generate migration SQL without DB connection
- Prisma enums use lowercase values: `admin`, `member`, `invited`, etc.
- migration_lock.toml must exist in prisma/migrations/ with `provider = "postgresql"`
- For incremental migrations without a DB, write SQL manually based on schema diff
- Use `bcryptjs` (pure JS) for bcrypt hashing; `jose` for JWT operations
- Prisma client singleton at lib/db.ts; session utils at lib/session.ts
- `npx prisma generate` must run before typecheck for @prisma/client types
- Next.js 14 App Router: cookies() is async, API routes use NextRequest/NextResponse

---

## Iteration 1 - US-001: Project Scaffolding
- What was implemented: Full Next.js 14+ project with App Router, TypeScript, Tailwind CSS, shadcn/ui, Prisma, ESLint, Prettier
- Files changed: package.json, tsconfig.json, next.config.mjs, tailwind.config.ts, postcss.config.mjs, components.json, .eslintrc.json, .prettierrc, .prettierignore, .gitignore, .env.example, app/globals.css, app/layout.tsx, app/page.tsx, lib/utils.ts, prisma/schema.prisma, types/index.ts, components/.gitkeep
- Learnings for future iterations:
  - Typecheck command: `npm run typecheck`
  - Lint command: `npm run lint`
  - shadcn/ui components go in `components/ui/`
  - Prisma schema at `prisma/schema.prisma` with postgresql provider
---

## Iteration 2 - US-002: Core database schema (Org, User, Domain)
- What was implemented: Org, User, Domain Prisma models with enums (UserRole, UserStatus, DomainStatus), relations, indexes, and migration SQL
- Files changed: prisma/schema.prisma, prisma/migrations/20240101000000_core_schema_org_user_domain/migration.sql, package.json (prisma downgrade to v5)
- Learnings for future iterations:
  - Had to downgrade prisma from v7 to v5 because v7 removed `url` from datasource block
  - `prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` generates SQL without needing DB
  - Migration files go in prisma/migrations/<timestamp>_<name>/migration.sql
  - UUIDs in Prisma 5: use `@id @default(uuid())` with `String` type (not native uuid)
  - Indexes added with `@@index([field])` for foreign keys
---

## Iteration 3 - US-003: Email and Thread database schema
- What was implemented: Thread and Email Prisma models with enums (Folder, EmailDirection, EmailStatus), relations, indexes, and migration SQL
- Files changed: prisma/schema.prisma (already had models from previous iteration setup), prisma/migrations/20240102000000_email_thread_schema/migration.sql, prisma/migrations/migration_lock.toml
- Learnings for future iterations:
  - `prisma migrate diff --from-migrations` requires a shadow database URL; not practical without a real DB
  - migration_lock.toml is required in the migrations directory (provider = "postgresql")
  - Thread and Email models were already defined in schema.prisma with correct fields from iteration 2
  - For incremental migrations, just write the SQL manually based on what's new vs existing migrations
  - JSONB type in SQL corresponds to `Json` in Prisma with `@default("[]")`
  - `@db.Text` annotation maps to TEXT column type in PostgreSQL
---

## Iteration 4 - US-004: Alias database schema
- What was implemented: Alias and AliasUser Prisma models with relations, unique constraint on (alias_id, user_id), indexes, FK from Email.delivered_via_alias to Alias, and migration SQL
- Files changed: prisma/schema.prisma, prisma/migrations/20240103000000_alias_schema/migration.sql
- Learnings for future iterations:
  - Email.delivered_via_alias was already a String? field; added optional relation to Alias model
  - Reverse relations needed on Org (aliases) and User (alias_users)
  - @@unique([alias_id, user_id]) enforces composite uniqueness in Prisma
  - Migration also adds FK constraint on existing Email.delivered_via_alias column
---

## Iteration 5 - US-005: Auth — signup endpoint
- What was implemented: POST /api/auth/signup endpoint with bcrypt password hashing, input validation, session cookie via JWT (jose), Prisma client singleton
- Files changed: package.json (added bcryptjs, jose, @types/bcryptjs), lib/db.ts (Prisma singleton), lib/session.ts (JWT session utils), app/api/auth/signup/route.ts
- Learnings for future iterations:
  - Use `bcryptjs` (pure JS) instead of `bcrypt` (native) to avoid build issues on Vercel
  - `jose` library for JWT: SignJWT and jwtVerify with HS256
  - Next.js 14 cookies() from next/headers is async (returns Promise)
  - Prisma client singleton pattern: store on globalThis to avoid re-creating in dev
  - Session stored as httpOnly cookie with 7 day expiry
  - `npx prisma generate` needed before typecheck so @prisma/client types exist
  - lib/session.ts exports: createSession, setSessionCookie, getSession, clearSessionCookie
  - lib/db.ts exports: prisma (singleton PrismaClient instance)
---

## Iteration 6 - US-006: Auth — login endpoint
- What was implemented: POST /api/auth/login endpoint with bcrypt password verification, session cookie on success, generic 401 for invalid credentials, status=active check
- Files changed: app/api/auth/login/route.ts (new file)
- Learnings for future iterations:
  - Login follows same pattern as signup: parse body, validate, query DB, bcrypt.compare, createSession + setSessionCookie
  - Use generic "Invalid email or password" for all auth failures (prevents user enumeration)
  - Check user.status !== "active" before verifying password to block invited/disabled users
  - No new dependencies needed — bcryptjs, jose, prisma already installed
---

## Iteration 7 - US-007: Auth — session middleware and logout
- What was implemented: Next.js middleware.ts for route protection, getCurrentUser() helper, POST /api/auth/logout endpoint
- Files changed: middleware.ts (new), lib/session.ts (added getCurrentUser), app/api/auth/logout/route.ts (new), PRD.md
- Learnings for future iterations:
  - Next.js middleware.ts goes at project root (not inside app/)
  - middleware `config.matcher` uses regex-like patterns: `"/api/((?!auth/).*)"` to exclude auth routes
  - getCurrentUser() fetches user from DB using session.user_id and returns null if not found or inactive
  - Middleware can differentiate API vs page routes: return 401 JSON for API, redirect for pages
  - jose jwtVerify is available in Edge Runtime (middleware runs on Edge)
  - No need to duplicate getSecret() — middleware has its own copy since it can't import from lib/session.ts in all cases
---

## Iteration 8 - US-008: Auth — password reset flow
- What was implemented: POST /api/auth/forgot-password (generates reset token, sends email via Resend), POST /api/auth/reset-password (validates token, updates password, clears token), added reset_token and reset_token_expires_at to User model
- Files changed: prisma/schema.prisma (added reset_token, reset_token_expires_at to User), prisma/migrations/20240104000000_user_reset_token/migration.sql, app/api/auth/forgot-password/route.ts (new), app/api/auth/reset-password/route.ts (new)
- Learnings for future iterations:
  - Use `crypto.randomBytes(32).toString("hex")` for secure token generation (Node.js built-in)
  - Always return generic success message on forgot-password to prevent user enumeration
  - Resend API: POST https://api.resend.com/emails with Bearer token auth
  - Reset token lookup uses `findFirst` with where clause on token field
  - After password reset, clear both reset_token and reset_token_expires_at (set to null)
  - NEXT_PUBLIC_APP_URL env var used for constructing reset links
---

## Iteration 9 - US-009: Auth UI — signup page
- What was implemented: Signup page at /signup with form (org name, your name, email, password, confirm password), client-side validation, API submission, error display, link to login
- Files changed: app/signup/page.tsx (new), components/ui/button.tsx (new), components/ui/input.tsx (new), components/ui/label.tsx (new), components/ui/card.tsx (new)
- Learnings for future iterations:
  - shadcn/ui components created manually (Button, Input, Label, Card) since no CLI available
  - "use client" directive needed for pages with useState/useRouter
  - Form validation pattern: validate() returns boolean, sets errors state object
  - API error shown in a destructive-colored banner div
  - useRouter from next/navigation for App Router (not next/router)
  - components/.gitkeep can be removed once real component files exist
---

## Iteration 10 - US-010: Auth UI — login page
- What was implemented: Login page at /login with form (email, password), client-side validation, API submission to /api/auth/login, error display, links to signup and forgot-password pages
- Files changed: app/login/page.tsx (new)
- Learnings for future iterations:
  - Login page follows same Card-based layout pattern as signup page
  - Redirects to /inbox on success (not /dashboard like signup)
  - Uses same shadcn/ui components (Button, Input, Label, Card) already created
  - CardFooter uses flex-col with space-y for stacked links (forgot-password + signup)
  - Use &apos; entity for apostrophe in JSX text to satisfy linting
---

## Iteration 11 - US-011: Auth UI — password reset pages
- What was implemented: Forgot-password page at /forgot-password (email input, submits to API, shows success message), Reset-password page at /reset-password?token=xxx (new password + confirm fields, submits to reset API, redirects to login)
- Files changed: app/forgot-password/page.tsx (new), app/reset-password/page.tsx (new)
- Learnings for future iterations:
  - useSearchParams() requires Suspense boundary in Next.js App Router (SSR bailout)
  - Pattern: wrap component using useSearchParams in Suspense with a loading fallback
  - Forgot-password shows success state after submission (no redirect) using boolean toggle
  - Reset-password validates token presence on mount, shows error if missing
  - Same Card-based layout pattern consistent across all auth pages
---

## Iteration 12 - US-012: App layout shell
- What was implemented: Authenticated app layout with sidebar navigation, mobile hamburger menu (Sheet), user info in footer, admin-only nav items, logout button, placeholder pages for inbox/sent/search/settings/dashboard
- Files changed: components/ui/sheet.tsx (new), components/ui/separator.tsx (new), components/app-sidebar.tsx (new), components/app-shell.tsx (new), app/(app)/layout.tsx (new), app/(app)/inbox/page.tsx (new), app/(app)/sent/page.tsx (new), app/(app)/search/page.tsx (new), app/(app)/settings/page.tsx (new), app/(app)/dashboard/page.tsx (new)
- Learnings for future iterations:
  - Route groups `(app)` in Next.js App Router don't affect URLs but share a layout
  - AppShell is a client component ("use client") wrapping the sidebar + main content
  - Layout.tsx (server component) calls getCurrentUser() and passes user data to AppShell
  - Sheet component implemented manually (custom, not radix) with context for open/close state
  - Mobile sidebar uses Sheet with side="left", desktop sidebar is always visible via md:block
  - Active nav item determined by pathname matching with usePathname()
  - Admin nav items (Team, Domain, Aliases) conditionally shown based on user.role === "admin"
  - Dashboard redirects to /inbox (signup redirects to /dashboard initially)
---

## Iteration 13 - US-012: App layout shell (browser verification)
- What was implemented: Verified that the app layout shell works correctly - typecheck passes, build succeeds, dev server boots without errors
- Files changed: PRD.md (marked task complete)
- Learnings for future iterations:
  - For "Verify changes work in browser" tasks: confirm typecheck passes, build succeeds, and dev server starts
  - Next.js build output shows all routes and their types (static vs dynamic)
  - macOS doesn't have `timeout` command; use background process + sleep + kill pattern instead
---

## Iteration 14 - US-013: Domain — add domain API
- What was implemented: POST /api/domains endpoint — admin-only, validates domain format (regex for valid TLD, no protocol/path), creates Domain record with status=pending, calls Resend API to register domain, returns DNS records (type, name, value, priority), returns 409 if domain already registered
- Files changed: app/api/domains/route.ts (new)
- Learnings for future iterations:
  - Resend domain registration: POST https://api.resend.com/domains with { name: domain }
  - Resend returns { id, records: [...] } where each record has type, name, value, priority, status
  - Domain validation regex: /^(?!-)([a-zA-Z0-9-]{1,63}(?<!-)\.)+[a-zA-Z]{2,}$/ — validates proper TLD format
  - Admin-only endpoints: check user.role !== "admin" and return 403
  - getCurrentUser() already returns null for unauthenticated (handled by middleware), but double-check in route
  - Pattern: validate input → check conflicts (409) → call external API → save to DB → return response
---

## Iteration 15 - US-014: Domain — verification API
- What was implemented: POST /api/domains/:id/verify endpoint — admin-only, triggers Resend domain verification, checks MX/SPF/DKIM status from Resend records, updates Domain model flags, sets status to active when all verified
- Files changed: app/api/domains/[id]/verify/route.ts (new)
- Learnings for future iterations:
  - Next.js App Router dynamic routes: `[id]` folder name creates params.id
  - In Next.js 14+, route handler params is a Promise that must be awaited: `const { id } = await params`
  - Resend verify endpoint: POST /domains/{domain}/verify triggers verification check
  - Resend domain status: GET /domains/{domain} returns records array with status per record
  - SPF records identified by TXT type with name containing "_spf" or value containing "spf"
  - DKIM records identified by CNAME/TXT type with name containing "dkim"
  - Use `findFirst` with org_id filter to ensure domain belongs to user's org (security)
  - DomainStatus enum values: "pending", "verified", "active" — cast string with `as` for type safety
---

## Iteration 16 - US-015: Domain — setup UI (admin)
- What was implemented: Domain settings page at /settings/domain with add domain form, DNS records table (Type, Name, Value, Priority, Status columns), Copy button per record, Verify Domain button, status badges (pending/yellow, verified/green, failed/red), loading state, and GET /api/domains endpoint
- Files changed: app/(app)/settings/domain/page.tsx (new), components/ui/badge.tsx (new), app/api/domains/route.ts (added GET handler)
- Learnings for future iterations:
  - Badge component uses cva (class-variance-authority) with custom variants: success (green), warning (yellow), destructive (red)
  - Domain page uses client-side state machine: loading → no domain (show form) → domain exists (show DNS records + verify button)
  - Copy to clipboard: `navigator.clipboard.writeText(value)` with visual feedback via state
  - DNS records table uses font-mono for technical values, truncate for long values
  - GET /api/domains fetches first domain for the org (MVP: single domain per org)
  - Build output shows /settings/domain as dynamic route (3.76 kB)
---

## Iteration 17 - US-016: Send email — API endpoint
- What was implemented: POST /api/emails/send endpoint — accepts to/cc/bcc/subject/body_html/body_plain/in_reply_to/references, sends via Resend API, creates Email record (outbound, sent), creates or updates Thread, generates Message-ID header, returns 400 if org domain not active
- Files changed: app/api/emails/send/route.ts (new)
- Learnings for future iterations:
  - Prisma nullable JSON fields require `Prisma.JsonNull` instead of plain `null` for type safety
  - Import `{ Prisma }` from `@prisma/client` to access `Prisma.JsonNull`
  - Resend send email API: POST https://api.resend.com/emails with from/to/subject/html/text/headers
  - Custom headers (Message-ID, In-Reply-To, References) go in `headers` object of Resend payload
  - Thread matching on reply: find existing email by message_id matching in_reply_to, use its thread_id
  - New outbound emails set folder="sent" on both Thread and Email records
  - participant_emails stored as JSON.stringify of unique address array
  - `randomUUID()` from Node.js crypto for generating message IDs
---

## Iteration 18 - US-017: Send email — delivery status webhook
- What was implemented: POST /api/webhooks/resend webhook handler that handles email.sent, email.delivered, email.bounced, email.delivery_delayed events, verifies Svix/HMAC signature, updates Email record status, ignores duplicates gracefully, returns 200 immediately
- Files changed: app/api/webhooks/resend/route.ts (new), .env.example (added RESEND_WEBHOOK_SECRET)
- Learnings for future iterations:
  - Resend uses Svix for webhook delivery — headers are svix-id, svix-timestamp, svix-signature
  - Svix signature: HMAC-SHA256 of "<msg_id>.<timestamp>.<body>" with base64-encoded key (after stripping "whsec_" prefix)
  - Signature header contains space-separated "v1,<base64sig>" entries
  - Use timingSafeEqual for constant-time signature comparison (prevent timing attacks)
  - Status priority prevents downgrading (e.g., delivered won't go back to sent)
  - Email matching: try message_id contains or direct id match against Resend's email_id
  - Webhook route does NOT require auth middleware (external service calls it)
  - RESEND_WEBHOOK_SECRET env var added for signing secret
---

## Iteration 19 - US-018: Receive email — inbound webhook handler
- What was implemented: Added `email.received` event handling to the existing webhook at POST /api/webhooks/resend. Parses from/to/cc/subject/html/text/message_id/in_reply_to/references/attachments from Resend webhook payload. Routes to correct user by matching to address against User.email. Creates Email record (direction=inbound, status=received, read=false) and a new Thread per message. Stores attachment metadata (filename, content_type, size) as JSON.
- Files changed: app/api/webhooks/resend/route.ts (major refactor: split into handleInboundEmail + handleDeliveryStatus functions), PRD.md
- Learnings for future iterations:
  - Refactored webhook handler into two functions: handleInboundEmail() and handleDeliveryStatus()
  - Resend inbound webhook payload has: from, to, cc, subject, html, text, message_id, in_reply_to, references, attachments
  - to/cc can be string or string[] — normalize with Array.isArray check
  - references can be space-separated string or array — handle both formats
  - Email addresses may be in "Name <email>" format — extract with regex /<([^>]+)>/
  - Thread subject strips Re:/Fwd:/Fw: prefix for clean threading
  - Duplicate prevention: check message_id + user_id + direction=inbound before creating
  - Basic thread creation per email (US-019 will handle proper threading logic)
  - Renamed EVENT_STATUS_MAP to DELIVERY_STATUS_MAP for clarity
---

## Iteration 20 - US-019: Receive email — threading logic
- What was implemented: Proper email threading in handleInboundEmail — checks In-Reply-To and References headers to find existing threads, updates thread metadata (last_message_at, message_count, unread_count), merges participant_emails, and creates new threads only when no match found
- Files changed: app/api/webhooks/resend/route.ts (refactored handleInboundEmail, added findExistingThread helper and cleanSubject helper)
- Learnings for future iterations:
  - findExistingThread() queries Email.message_id with `{ in: messageIdsToCheck }` to match against both inbound and outbound emails
  - In-Reply-To is checked first, then References in reverse order (most recent first) for priority
  - Thread.participant_emails is stored as JSON string; parse it back to merge new participants
  - Prisma `{ increment: 1 }` syntax for atomic counter updates on message_count/unread_count
  - cleanSubject() uses /^(Re|Fwd|Fw):\s*/gi with global+case-insensitive to strip all prefixes
  - Thread update and participant merge done in two separate updates (first for counters, second for participants after merge logic)
---

## Iteration 21 - US-020: Receive email — alias routing
- What was implemented: Alias routing for inbound emails — when a `to` address matches an Alias, deliver to all AliasUser members with delivered_via_alias set, preventing duplicate delivery if user is both directly addressed and on the alias
- Files changed: app/api/webhooks/resend/route.ts (extracted deliverToUser helper, added alias matching phase)
- Learnings for future iterations:
  - Extracted `deliverToUser()` helper to avoid code duplication between direct and alias delivery
  - Two-phase delivery: Phase 1 (direct user match) → Phase 2 (alias match), with deliveredUserIds Set to prevent duplicates
  - Alias lookup uses `prisma.alias.findFirst` with `include: { alias_users: { include: { user: true } } }` for nested data
  - deliverToUser takes an optional `aliasId` param to set delivered_via_alias on the Email record
  - Only deliver to alias users with status="active" (skip disabled/invited users)
  - Duplicate prevention across direct+alias: check deliveredUserIds before delivering via alias
---

## Iteration 22 - US-021: Receive email — catch-all routing
- What was implemented: Phase 3 catch-all routing in handleInboundEmail — after direct user + alias matching, unmatched addresses are checked against org domains. If catch_all_enabled, deliver to all org admins with original_to set.
- Files changed: app/api/webhooks/resend/route.ts (added Phase 3 catch-all logic, added originalTo param to deliverToUser, added matchedAddresses tracking)
- Learnings for future iterations:
  - Catch-all requires extracting domain from email address and looking up Domain → Org
  - matchedAddresses Set tracks which `to` addresses were resolved by Phase 1 (direct) or Phase 2 (alias)
  - deliverToUser now accepts optional `originalTo` param, stored in Email.original_to field
  - When catch_all_enabled is false, simply skip the address (Resend will handle bounce externally)
  - Phase 1 user-check is quick (findFirst), but Phase 3 re-checks weren't needed; used matchedAddresses Set instead
  - Domain lookup uses `include: { org: true }` to access org.catch_all_enabled in one query
---

## Iteration 23 - US-022: WebSocket server setup
- What was implemented: WebSocket server (ws library) as separate process, authenticated via session JWT cookie, user connection map, notifyUser() helper, internal HTTP API for cross-process notification, keep-alive pings
- Files changed: lib/ws-server.ts (new - core WS server logic), lib/notify.ts (new - notifyUser helper for Next.js process), server/ws.ts (new - standalone WS server entry point with internal HTTP API), package.json (added ws dep + ws:dev script), .env.example (added WS_PORT, WS_HTTP_PORT, WS_INTERNAL_URL, NEXT_PUBLIC_WS_URL)
- Learnings for future iterations:
  - WebSocket server runs as separate process (not inside Next.js) for Vercel compatibility
  - Architecture: Next.js → HTTP POST to WS internal API → WS server broadcasts to connected clients
  - lib/ws-server.ts: createWebSocketServer() + notifyUser() for in-process use
  - lib/notify.ts: notifyUser() for cross-process use (posts to WS HTTP API)
  - WS server authenticates by parsing session cookie from upgrade request headers
  - Use Set.forEach() instead of for...of on Sets to avoid downlevelIteration tsconfig issues
  - Clients map: Map<userId, Set<ConnectedClient>> supports multiple tabs per user
  - Keep-alive via ping every 30 seconds to detect stale connections
  - `npx tsx server/ws.ts` runs the TypeScript server directly for dev
---

## Iteration 24 - US-023: Real-time email push to client
- What was implemented: notifyUser() call in webhook handler after inbound email delivery, useWebSocket() client hook with exponential backoff reconnection, subscribe() API for multiple handlers
- Files changed: app/api/webhooks/resend/route.ts (import notifyUser, call after email created), hooks/use-websocket.ts (new client hook)
- Learnings for future iterations:
  - Client hooks go in `hooks/` directory (not `lib/` which is server-side)
  - useWebSocket uses refs for mutable state that shouldn't trigger re-renders (handlers, reconnect timer, ws instance)
  - Exponential backoff: `1000 * 2^attempts` capped at 30s max delay
  - WebSocket.onclose fires on both clean close and errors; reconnect in onclose only
  - notifyUser payload should be minimal: threadId, emailId, subject, from, preview (first 100 chars of bodyPlain)
  - "use client" directive required for hooks using React state/effects
  - Set.forEach used instead of for...of to avoid downlevelIteration tsconfig requirement
  - NEXT_PUBLIC_WS_URL env var used client-side for WebSocket connection URL
---

## Iteration 26 - US-025: Thread view UI
- What was implemented: GET /api/threads/:id endpoint (returns all emails in thread, marks as read), Thread view page at /inbox/[threadId] with collapsible messages, DOMPurify HTML sanitization, back button, skeleton loading, error state
- Files changed: app/api/threads/[id]/route.ts (new), app/(app)/inbox/[threadId]/page.tsx (new), package.json (added isomorphic-dompurify, @types/dompurify)
- Learnings for future iterations:
  - isomorphic-dompurify works on both server and client (wraps DOMPurify)
  - DOMPurify.sanitize() with ALLOWED_TAGS/ALLOWED_ATTR for safe HTML rendering
  - dangerouslySetInnerHTML renders sanitized HTML — use prose class for nice typography
  - Thread read marking: update Thread.unread_count=0 and Email.read=true for all emails in thread
  - EmailMessageItem component with defaultExpanded prop (last message expanded, rest collapsed)
  - Chronological order (oldest first) with `orderBy: { received_at: "asc" }` in Prisma
  - Next.js dynamic route params must be awaited in route handlers: `const { id } = await params`
  - Build shows 11 kB for thread view page (includes DOMPurify)
---

## Iteration 25 - US-024: Inbox list view UI
- What was implemented: GET /api/threads endpoint (paginated, folder-filtered, sorted by last_message_at desc), Inbox page UI with thread list (sender avatar/initial, name, subject, preview, time, unread indicators), skeleton loading state, empty state, pagination controls, real-time refresh via WebSocket new_email events
- Files changed: app/api/threads/route.ts (new), app/(app)/inbox/page.tsx (rewritten from placeholder)
- Learnings for future iterations:
  - GET /api/threads uses `include: { emails: { take: 1, orderBy: { received_at: "desc" } } }` to get latest email for preview
  - Thread summary includes: id, subject, unread_count, starred, message_count, last_message_at, from_address (from latest email), preview (first 120 chars of body_plain)
  - formatTime() shows: time for today, weekday for this week, "Mon DD" for older
  - extractName() parses "Name <email>" format or falls back to local part before @
  - Unread threads: bold sender name, blue dot indicator, bg-blue-50/50 background
  - Pagination: 20 per page, Previous/Next buttons, page X of Y display
  - useWebSocket().subscribe() for real-time inbox refresh on new_email event
  - Skeleton loading: 6 placeholder rows with animate-pulse circles and bars
---

## Iteration 27 - US-026: Compose email — modal UI
- What was implemented: Compose modal with Dialog component, To/CC/BCC fields (CC/BCC toggled by button), Subject input, rich text body editor (contentEditable with bold/italic/link/list toolbar), Send button (calls /api/emails/send), toast notifications on success, inline error on failure, Compose button in sidebar
- Files changed: components/ui/dialog.tsx (new), components/ui/textarea.tsx (new), components/ui/toast.tsx (new - ToastProvider + useToast), components/compose-modal.tsx (new), components/app-sidebar.tsx (added Compose button + onCompose prop), components/app-shell.tsx (integrated ComposeModal + ToastProvider)
- Learnings for future iterations:
  - Dialog component uses React context for open/onOpenChange state (no Radix dependency)
  - ToastProvider wraps children and renders toast list via portal-like fixed positioning
  - useToast() hook provides addToast(message, variant) — auto-removes after 4s
  - Rich text editing uses contentEditable div with document.execCommand for formatting
  - execCommand supports: bold, italic, createLink, insertUnorderedList, insertOrderedList
  - ComposeModal resets form state on successful send or discard
  - Compose button added to sidebar between header and nav, passes onCompose callback
  - AppShell manages composeOpen state and passes it to both sidebar instances (desktop + mobile)
---

## Iteration 28 - US-027: Reply and forward in thread
- What was implemented: Reply, Reply All, and Forward buttons on each message in thread view. Inline reply form at bottom of thread with rich text toolbar. Forward opens compose modal with quoted body and Fwd: subject. Sent replies appear immediately in thread (optimistic update). Thread API now returns message_id for proper In-Reply-To/References header threading.
- Files changed: app/(app)/inbox/[threadId]/page.tsx (rewritten with Reply/ReplyAll/Forward buttons, InlineReplyForm component), app/api/threads/[id]/route.ts (added message_id to email select and response), components/compose-modal.tsx (added ComposePreFill export, preFill prop with useEffect)
- Learnings for future iterations:
  - Reply pre-fills To with extractEmail(from_address), subject with "Re: " prefix, in_reply_to with message_id, references with all prior message_ids
  - Reply All puts original sender in To, all other to+cc participants in CC (deduplicated)
  - Forward uses ComposeModal with preFill prop (to="", subject="Fwd: ...", bodyHtml=quoted original)
  - buildQuotedHtml() wraps original in styled blockquote div with "On [date], [from] wrote:" header
  - Optimistic update: after successful reply send, append sentEmail to thread.emails state immediately
  - ComposePreFill interface exported from compose-modal.tsx for use by other components
  - useEffect with [open, preFill] dependencies applies preFill values when modal opens
  - InlineReplyForm is a separate component within the thread page using useToast() from parent ToastProvider
  - message_id needed in thread API response for building proper References header chain
---

## Iteration 29 - US-028: Sent folder view
- What was implemented: Sent folder page at /sent showing threads with folder=sent, displaying recipient names (To: Name), subject, preview, time, with pagination. Updated GET /api/threads to include to_addresses in the response.
- Files changed: app/(app)/sent/page.tsx (rewritten from placeholder), app/api/threads/route.ts (added to_addresses to select and response)
- Learnings for future iterations:
  - Sent page reuses same layout pattern as inbox but shows "To: [recipient]" instead of sender name
  - to_addresses is stored as JSON in DB; API returns it as-is from the email's to_addresses field
  - getRecipientDisplay() handles multiple recipients: shows first name + count of others
  - Sent threads don't show unread indicators (no bold/blue dot) since user sent them
  - Thread click navigates to /inbox/[threadId] — same thread view works for both inbox and sent
  - Shell glob issues with parentheses in paths: quote path like "app/(app)/sent/page.tsx" for git add
  - cn() utility imported but only minimally used in sent view (no conditional styling needed)
---

## Iteration 30 - US-030: Email actions — star and read/unread API
- What was implemented: Three new API endpoints — PATCH /api/threads/:id/star (toggles starred boolean), PATCH /api/threads/:id/read (marks all emails as read, sets unread_count=0), PATCH /api/threads/:id/unread (sets unread_count=1)
- Files changed: app/api/threads/[id]/star/route.ts (new), app/api/threads/[id]/read/route.ts (new), app/api/threads/[id]/unread/route.ts (new)
- Learnings for future iterations:
  - Follow existing archive/route.ts pattern: getCurrentUser() → findFirst with user_id → update → return updated thread JSON
  - Star toggles via `!thread.starred` (reads current then inverts)
  - Read endpoint uses Promise.all to update thread.unread_count=0 and email.read=true simultaneously
  - Unread endpoint simply sets thread.unread_count=1 (no need to update individual emails)
  - All endpoints return consistent response shape: id, subject, folder, starred, unread_count, message_count, last_message_at
---

## Iteration 31 - US-031: Email actions — UI controls
- What was implemented: Action buttons in thread view header (Archive, Trash, Star, Mark Unread), hover actions in inbox list (Archive, Trash, Star), toast notifications with Undo action, optimistic UI updates
- Files changed: app/(app)/inbox/[threadId]/page.tsx (added action buttons header + handlers), app/(app)/inbox/page.tsx (added hover actions), components/ui/toast.tsx (added ToastAction support with action button)
- Learnings for future iterations:
  - ToastAction interface with { label, onClick } enables undo functionality in toasts
  - Toast with action gets longer timeout (6s vs 4s) to give user time to click Undo
  - Optimistic UI: update state immediately, then revert on API failure
  - Group hover actions use `group` class on parent + `group-hover:flex` / `group-hover:hidden` for show/hide
  - Thread view actions navigate back to /inbox after archive/trash/mark-unread for better UX
  - Changed inbox row from <button> to <div role="button"> to nest buttons properly (hover actions)
  - e.stopPropagation() on action button clicks prevents row click from firing
  - Star icon uses conditional fill: `fill-yellow-400 text-yellow-400` when starred
---

## Iteration 32 - US-032: Trash auto-purge background job
- What was implemented: POST /api/cron/purge-trash endpoint that permanently deletes emails where folder=trash AND trash_expires_at < now, plus empty threads. Protected by CRON_SECRET. Vercel cron configuration added.
- Files changed: app/api/cron/purge-trash/route.ts (new), vercel.json (new), .env.example (added CRON_SECRET)
- Learnings for future iterations:
  - Vercel Cron jobs configured in vercel.json with `crons` array containing `path` and `schedule`
  - Cron schedule "0 3 * * *" runs at 3 AM UTC daily
  - Protect cron endpoints with Bearer token auth via CRON_SECRET env var
  - Use `Array.from(new Set(...))` instead of `[...new Set(...)]` to avoid downlevelIteration tsconfig issue
  - After deleting emails, check if thread is empty and delete it; otherwise update message_count
  - deleteMany returns `{ count: number }` for logging purposes
---

## Iteration 33 - US-033: File attachments — upload on send
- What was implemented: S3 upload API endpoint at POST /api/attachments/upload, compose modal with attach button and drag-drop zone on body editor, file chips display with filename/size/remove button, 50MB client+server validation, send API updated to pass attachment URLs to Resend
- Files changed: package.json (added @aws-sdk/client-s3), app/api/attachments/upload/route.ts (new), components/compose-modal.tsx (rewritten with attachments support), app/api/emails/send/route.ts (added attachments handling), .env.example (added S3 env vars)
- Learnings for future iterations:
  - @aws-sdk/client-s3 for S3 uploads with PutObjectCommand
  - S3Client supports endpoint option for S3-compatible services (MinIO, etc.)
  - Resend attachments via path URL: `attachments: [{ filename, path: url }]`
  - Compose modal drag-drop: onDragOver sets isDragging true, onDrop handles files, onDragLeave sets false
  - FormData.getAll("files") returns File[] when multiple files uploaded
  - Attachment state uses temp IDs while uploading, replaced with server IDs on success
  - Prisma Json fields: store attachments as array of objects, use Prisma.JsonNull for empty
  - File size formatting: bytes → KB → MB with toFixed(1)
---

## Iteration 34 - US-034: File attachments — display on received emails
- What was implemented: Attachments display in thread view showing filename, size, and file type icon. Download API endpoint at GET /api/attachments/[emailId]/[attachmentId]. Image attachments show inline thumbnail preview. Non-image files show appropriate icons (FileText, FileAudio, FileVideo, FileArchive).
- Files changed: app/(app)/inbox/[threadId]/page.tsx (added AttachmentItem + AttachmentsList components), app/api/attachments/[emailId]/[attachmentId]/route.ts (new), app/api/webhooks/resend/route.ts (updated to store attachment id and content)
- Learnings for future iterations:
  - Resend inbound webhooks may include `content` (base64) for attachments
  - AttachmentItem stores attachment with id, filename, content_type, size, content (base64), url
  - Download API decodes base64 content and returns as file with proper Content-Disposition header
  - For S3-hosted attachments (outbound), redirect to URL instead of proxying
  - Image preview: create data URL from base64 `data:${content_type};base64,${content}`
  - getFileIcon() helper maps content_type to appropriate lucide-react icons
  - Use `as unknown as Type` for Prisma JsonArray to custom type conversion
  - Grid layout `sm:grid-cols-2` for attachment list on larger screens
---

## Iteration 35 - US-035: Search — full-text search API
- What was implemented: GET /api/emails/search endpoint using PostgreSQL full-text search with tsvector/tsquery, GIN index, automatic search_vector update trigger, filtering by folder/from/date_range, snippet generation with context
- Files changed: prisma/schema.prisma (added search_vector Unsupported("tsvector")?), prisma/migrations/20240105000000_email_fulltext_search/migration.sql (new), app/api/emails/search/route.ts (new)
- Learnings for future iterations:
  - Prisma doesn't natively support tsvector; use `Unsupported("tsvector")?` for the column type
  - PostgreSQL full-text search uses setweight() to prioritize: 'A' for subject, 'B' for body, 'C' for from
  - Create trigger with `BEFORE INSERT OR UPDATE` to auto-update search_vector column
  - Use $queryRawUnsafe for complex raw SQL with ts_rank() ordering
  - tsquery format: join terms with " & " for AND matching (e.g., "hello & world")
  - Snippet creation: find first match position, extract surrounding context with ellipsis
  - Filter params use parameterized queries with incrementing $N placeholders for SQL injection safety
  - Max 50 results ordered by ts_rank DESC then received_at DESC for relevance + recency
---

## Iteration 36 - US-036: Search — UI
- What was implemented: Search page at /search with search input (search icon, loading spinner), 300ms debounced search + Enter key trigger, results list with sender/subject/snippet/date displayed, highlighted search terms in subject and snippet, empty state for no results, initial state prompt
- Files changed: app/(app)/search/page.tsx (rewritten from placeholder)
- Learnings for future iterations:
  - highlightMatch() function splits text by regex pattern containing search terms, wraps matches in <mark> tags
  - escapeRegex() needed to safely create RegExp from user input (escape special chars)
  - searchedQuery state tracks the query used for highlighting (vs current input value)
  - useCallback for performSearch function to use in useEffect dependency array
  - line-clamp-2 utility truncates snippet to 2 lines (Tailwind CSS)
  - Mail icon from lucide-react used as avatar for search results
  - Two empty states: "No results found" (searched=true, results=[]) vs "Search your emails" (searched=false)
---

## Iteration 37 - US-037: User invite — API
- What was implemented: POST /api/users/invite (admin-only, creates invited user with token, sends email), POST /api/users/:id/reinvite (admin-only, regenerates token and resends email)
- Files changed: app/api/users/invite/route.ts (new), app/api/users/[id]/reinvite/route.ts (new)
- Learnings for future iterations:
  - User invite creates User with status=invited, empty password_hash (set on claim)
  - invite_token generated with crypto.randomBytes(32).toString("hex")
  - invite_expires_at set to 7 days from creation
  - Email domain validation: check if active domain exists, compare with email domain
  - Reinvite endpoint only works for users with status=invited (returns 400 otherwise)
  - Invite email contains /claim?token=xxx link for user to set password
  - Both endpoints use getCurrentUser() + role check for admin-only access
---

## Iteration 38 - US-038: User invite — claim account page
- What was implemented: Page at /claim?token=xxx with token validation (GET /api/auth/claim/validate), welcome message showing user name and email, password/confirm password form, POST /api/auth/claim endpoint that validates token, hashes password, sets status=active, claimed_at=now, clears token, and sets session cookie
- Files changed: app/api/auth/claim/route.ts (new), app/api/auth/claim/validate/route.ts (new), app/claim/page.tsx (new), PRD.md
- Learnings for future iterations:
  - Added separate validation endpoint GET /api/auth/claim/validate?token=xxx to fetch user info before showing form
  - Token validation checks: exists, not expired, status=invited (not already claimed)
  - Claim endpoint sets session cookie on success, redirects to /inbox
  - useCallback for validateToken function to use in useEffect dependency array
  - Same Suspense pattern with useSearchParams as other auth pages
---

## Iteration 39 - US-039: Admin — user management UI
- What was implemented: Team management page at /settings/team (admin-only) with user table showing name, email, role, status (active/invited/disabled), joined date. Invite User dialog with email/name/role fields. Reinvite button for invited users, Disable button for active users. Cannot disable yourself check.
- Files changed: app/(app)/settings/team/page.tsx (new), app/api/users/route.ts (new), app/api/users/[id]/disable/route.ts (new), PRD.md
- Learnings for future iterations:
  - GET /api/users returns all org users with select fields (admin-only endpoint)
  - PATCH /api/users/:id/disable sets status=disabled, returns 400 if trying to disable yourself
  - Team page uses InviteUserDialog component with form state reset on success/cancel
  - Toast variant is "destructive" not "error" in this codebase
  - Uses same Badge component with success/warning/destructive variants for status display
  - isInviteExpired() helper checks if invite_expires_at < now
---

## Iteration 40 - US-040: RBAC middleware enforcement
- What was implemented: Added requireAdmin() helper in lib/session.ts that returns 401 for unauthenticated or 403 for non-admin users. Applied it to all admin-only endpoints (domains, user invite/manage). Verified email/thread queries are scoped by user_id. Sidebar already hides admin nav items for members.
- Files changed: lib/session.ts (added requireAdmin), app/api/domains/route.ts, app/api/domains/[id]/verify/route.ts, app/api/users/route.ts, app/api/users/invite/route.ts, app/api/users/[id]/reinvite/route.ts, app/api/users/[id]/disable/route.ts
- Learnings for future iterations:
  - requireAdmin() pattern: `const result = await requireAdmin(); if ("error" in result) return result.error; const user = result;`
  - Returns a union type: `CurrentUser | { error: Response }` to distinguish success from error
  - Dynamic import of NextResponse inside requireAdmin() to avoid importing server module in potential edge runtime
  - All email/thread queries already use `user_id: user.id` filter (inbox list, thread view, search, actions)
  - Sidebar hides admin items via `isAdmin && (<>...</>)` conditional in app-sidebar.tsx
---

## Iteration 41 - US-041: Alias CRUD API
- What was implemented: Complete alias CRUD API with 6 endpoints: POST /api/aliases (create alias), GET /api/aliases (list all org aliases with assigned users), PATCH /api/aliases/:id (update name and users), DELETE /api/aliases/:id (delete alias and AliasUser records), POST /api/aliases/:id/users (add user to alias), DELETE /api/aliases/:id/users/:userId (remove user from alias)
- Files changed: app/api/aliases/route.ts (new), app/api/aliases/[id]/route.ts (new), app/api/aliases/[id]/users/route.ts (new), app/api/aliases/[id]/users/[userId]/route.ts (new), PRD.md
- Learnings for future iterations:
  - Alias address validation: extract domain with .split("@")[1], check against org's active domain
  - AliasUser uses composite unique constraint: alias_id_user_id (accessible via where clause)
  - PATCH endpoint can update both name and users array in one call (delete all + recreate pattern for users)
  - Use findFirst with org_id filter to ensure alias belongs to user's org before any operations
  - createMany for bulk alias user creation when updating users list
---

## Iteration 42 - US-042: Alias — send as alias
- What was implemented: Compose modal From dropdown for alias selection, API endpoint to get user's sendable aliases, send email API supports from_alias_id, sent_as_alias field in Email schema, auto-select alias in reply/forward if delivered via alias
- Files changed: prisma/schema.prisma (added sent_as_alias), prisma/migrations/20240106000000_sent_as_alias/migration.sql (new), app/api/users/me/aliases/route.ts (new), app/api/emails/send/route.ts (added from_alias_id handling), app/api/threads/[id]/route.ts (added delivered_via_alias to response), components/compose-modal.tsx (added From dropdown UI), app/(app)/inbox/[threadId]/page.tsx (auto-select alias in reply/forward)
- Learnings for future iterations:
  - Add sent_as_alias (not just delivered_via_alias) to track which alias was used for outbound emails
  - GET /api/users/me/aliases returns { personal_email, aliases: [...] } for dropdown options
  - Compose modal fetches aliases on open with IIFE async pattern (not function declaration in block)
  - Use `(async () => { ... })()` instead of `async function foo() {}` inside conditionals to avoid ES5 strict mode error
  - fromAliasId passed in ComposePreFill interface for auto-selection
  - Thread API returns delivered_via_alias so reply/forward can auto-select
  - InlineReplyForm passes from_alias_id from replyData.fromAliasId
---

## Iteration 43 - US-043: Alias — admin management UI
- What was implemented: Admin page at /settings/aliases with full alias CRUD UI — list of aliases (address, name, user count), expandable detail view showing assigned users with can_send_as toggle, Create Alias dialog with address prefix input (auto-appends @domain), Add User dropdown for assigning org users, Remove user button, Delete alias with confirmation dialog
- Files changed: app/(app)/settings/aliases/page.tsx (new)
- Learnings for future iterations:
  - Expandable rows: use expandedAliasId state + ChevronRight/ChevronDown toggle icons
  - AddUserDropdown component filters already-assigned users from org users list
  - Create alias dialog uses address prefix input with domain suffix shown in disabled span
  - can_send_as toggle uses PATCH /api/aliases/:id with full users array (delete + recreate pattern)
  - Delete confirmation uses separate dialog with deleteConfirmId state
  - Promise.all to fetch aliases, users, and domain data in parallel on mount
  - actionLoading state uses composite key like `${aliasId}-${userId}-toggle` for granular loading states
---

## Iteration 44 - US-044: Catch-all — admin toggle UI
- What was implemented: Catch-all routing toggle on domain settings page, PATCH /api/orgs/settings endpoint (admin-only), inbox list "catch-all" badge for emails delivered via catch-all with original address display
- Files changed: app/api/orgs/settings/route.ts (new), app/(app)/settings/domain/page.tsx (added catch-all toggle card), app/api/threads/route.ts (added original_to field), app/(app)/inbox/page.tsx (added catch-all badge and original address display)
- Learnings for future iterations:
  - Custom toggle switch: use button with role="switch", aria-checked, and translate-x classes for thumb position
  - requireAdmin() helper returns `{ error: Response }` union for admin-only endpoints
  - Org settings endpoint uses findUnique/update with org_id from authenticated user
  - Catch-all badge uses Badge variant="secondary" with amber color for original address text
  - original_to field on Email stored when catch-all routing delivers to admins
  - Thread API includes original_to from latest email for inbox list display
---

## Iteration 45 - US-045: Notifications — browser push setup
- What was implemented: Browser push notification system with service worker, permission prompt, and WebSocket integration
- Files changed: public/sw.js (new), hooks/use-notifications.ts (new), components/notification-prompt.tsx (new), components/notification-listener.tsx (new), components/app-shell.tsx (added notification components)
- Learnings for future iterations:
  - Service worker goes in public/sw.js for Next.js to serve it at /sw.js
  - Service worker handles notificationclick event to focus existing tab or open new one
  - Use ServiceWorkerRegistration.showNotification() instead of new Notification() for service worker features
  - TypeScript NotificationOptions doesn't include `renotify` or `data` props, but they work with SW - cast to NotificationOptions
  - Client posts message to service worker listener for notification click navigation
  - Check document.hasFocus() to avoid showing notifications when user is actively on the tab
  - NotificationPrompt uses localStorage key to persist dismissal across sessions
  - NotificationListener subscribes to WebSocket new_email events and calls showNotification
  - 2-second delay before showing prompt to avoid overwhelming user on first load
---

## Iteration 46 - US-046: Notifications — in-app indicators
- What was implemented: In-app notification indicators including tab title with unread count, sidebar badge, dynamic favicon with dot overlay, real-time count updates via WebSocket, and optional notification sound using Web Audio API
- Files changed: app/api/threads/unread-count/route.ts (new), contexts/notification-context.tsx (new), components/notification-indicators.tsx (new), components/app-shell.tsx (added NotificationProvider + NotificationIndicators), components/app-sidebar.tsx (added unread count badge), app/(app)/inbox/page.tsx (refresh unread count on actions)
- Learnings for future iterations:
  - NotificationContext provides global unread count state and refreshUnreadCount() function
  - Tab title updated via document.title in NotificationIndicators component
  - Dynamic favicon created using Canvas API: draw original favicon, overlay red dot when unread > 0
  - Web Audio API for notification sound: creates AudioContext + OscillatorNode + GainNode for sine wave beep
  - localStorage for notification-sound-enabled preference persistence
  - Sidebar badge shows unreadCount with "99+" for overflow, styled as pill shape with primary colors
  - Inbox page calls refreshUnreadCount() after archive/trash actions and when clicking unread threads
  - setTimeout(500ms) used for thread click refresh to ensure mark-as-read API completes first
---

## Iteration 47 - US-047: Notification preferences UI
- What was implemented: Notification preferences section on /settings page with browser notifications and notification sound toggles, stored in User model as JSON field notification_preferences, with PATCH /api/users/me/preferences endpoint
- Files changed: prisma/schema.prisma (added notification_preferences JSON field to User), prisma/migrations/20240107000000_user_notification_preferences/migration.sql (new), app/api/users/me/preferences/route.ts (new), app/(app)/settings/page.tsx (rewritten with Notifications section)
- Learnings for future iterations:
  - Prisma JSON fields need proper type casting: use parsePreferences() helper with JsonValue import from @prisma/client/runtime/library
  - Settings page pattern: fetch preferences on mount, sync sound preference with NotificationContext via setSoundEnabled
  - Browser notification toggle checks permission state and requests permission if not granted before saving preference
  - Toggle switch button uses role="switch", aria-checked, translate-x-5/translate-x-0 for position
  - JSON default in Prisma: @default("{\"browser_notifications\": true, \"notification_sound\": false}")
---

## Iteration 48 - US-048: User settings page
- What was implemented: Full user settings page with Profile section (name editable, email/role read-only), Password change form (current + new + confirm), plus existing Notifications section. Added GET/PATCH /api/users/me for profile, PATCH /api/users/me/password for password change.
- Files changed: app/api/users/me/route.ts (new), app/api/users/me/password/route.ts (new), app/(app)/settings/page.tsx (extended with Profile + Password sections)
- Learnings for future iterations:
  - Profile section uses parallel fetch for /api/users/me and /api/users/me/preferences on mount
  - Name save button disabled until changes detected via hasNameChanges comparison
  - Password change validates current password server-side with bcrypt.compare before updating
  - Password endpoint returns success message, not user object (no need to return password_hash)
  - Form reset after successful password change: clear all three password fields
  - Toast hook useToast() from existing toast component for success notifications
  - Read-only inputs styled with bg-muted class for visual distinction
---

## Iteration 49 - US-049: Recipient autocomplete
- What was implemented: GET /api/contacts/suggest endpoint that returns matching email addresses from user's sent/received history. RecipientAutocomplete component with chip-based input, dropdown suggestions, keyboard navigation. Compose modal updated to use autocomplete for To/CC/BCC fields.
- Files changed: app/api/contacts/suggest/route.ts (new), components/recipient-autocomplete.tsx (new), components/compose-modal.tsx (updated to use RecipientAutocomplete)
- Learnings for future iterations:
  - Build frequency map from all user emails: inbound from_address + outbound to/cc_addresses
  - Contact matching on both email and name (case-insensitive)
  - RecipientAutocomplete uses chips for selected emails with X button to remove
  - Debounce search at 300ms to avoid excessive API calls
  - Keyboard navigation: ArrowDown/ArrowUp for selection, Enter to add, Backspace to remove last chip
  - selectedIndex state tracks which suggestion is highlighted
  - Filter out already-selected emails from suggestions
  - Tab key adds valid email when input has content
  - Comma or space after valid email auto-adds it as chip
---

## Iteration 50 - US-050: Mobile responsiveness pass
- What was implemented: Comprehensive mobile responsiveness pass across the entire app
- Files changed: 
  - app/(app)/inbox/page.tsx (touch-friendly row heights, responsive padding, larger touch targets)
  - app/(app)/inbox/[threadId]/page.tsx (responsive header, stacked reply buttons, touch-friendly toolbar)
  - app/(app)/sent/page.tsx (touch-friendly rows, responsive pagination)
  - app/(app)/search/page.tsx (larger search input, touch-friendly results)
  - app/(app)/settings/page.tsx (single-column layout, larger form inputs, touch-friendly toggles)
  - components/app-shell.tsx (removed main padding to allow full-width content)
  - components/app-sidebar.tsx (larger nav items, touch-friendly compose button)
  - components/compose-modal.tsx (full-screen on mobile, stacked form fields, touch-friendly toolbar)
  - components/ui/dialog.tsx (full-screen on mobile, touch-friendly close button)
  - components/ui/sheet.tsx (added SheetHeader, SheetTitle, bottom variant)
- Learnings for future iterations:
  - Use min-h-[60px] or min-h-[44px] for touch-friendly rows
  - Use h-10/h-11 for mobile buttons, h-9 for desktop with sm: breakpoint
  - Stack form labels vertically on mobile with flex-col gap-1 sm:flex-row sm:items-center
  - Use overflow-x-auto for horizontal toolbars on mobile
  - Hide non-essential elements on mobile with hidden sm:inline or hidden sm:flex
  - Icons should be h-5 w-5 on mobile, h-4 w-4 on desktop
  - Padding should be px-3 sm:px-4 for edge-to-edge mobile content
  - Action buttons stack with flex-col-reverse gap-2 sm:flex-row (primary button first visually)
---

## Iteration 51 - US-051: Email HTML sanitization
- What was implemented: Enhanced DOMPurify configuration for comprehensive XSS protection plus external image blocking for tracking pixel protection
- Files changed: app/(app)/inbox/[threadId]/page.tsx
- Learnings for future iterations:
  - DOMPurify was already installed as isomorphic-dompurify from US-025
  - Use FORBID_ATTR with comprehensive list of all HTML event handlers (onclick, onerror, onload, etc.)
  - ALLOWED_URI_REGEXP blocks javascript:, vbscript:, and data: URLs (except safe patterns)
  - External image blocking: replace src with placeholder SVG, store original in data-blocked-src
  - hasExternalImages() helper uses regex to check for non-data:, non-cid: image sources
  - Show warning banner with "Load Images" button for user consent
  - Pass showExternalImages flag to sanitizeHtml() to conditionally process images
  - BLOCKED_IMAGE_PLACEHOLDER uses inline SVG data URL with "Image blocked" text
---

## Iteration 52 - US-052: Error handling and loading states
- What was implemented: Comprehensive error handling and loading states across the app
- Files changed:
  - components/connection-status.tsx (new - shows offline/reconnecting status bar)
  - components/app-shell.tsx (added ConnectionStatus component)
  - app/(app)/inbox/page.tsx (added error toast on API failure)
  - app/(app)/sent/page.tsx (added error toast on API failure)
  - app/(app)/search/page.tsx (added error toast on API failure)
  - app/error.tsx (already existed - global error boundary)
  - app/not-found.tsx (already existed - 404 page)
- Learnings for future iterations:
  - Most loading states were already implemented in previous iterations (skeletons, loaders)
  - ConnectionStatus component uses both navigator.onLine and WebSocket isConnected for offline detection
  - Use 3-second delay before showing reconnecting message to avoid flash during quick reconnects
  - Fixed bottom bar with z-50 for connection status visibility
  - useWebSocket hook already exposed isConnected state for real-time connection monitoring
  - useToast already used in inbox page, just needed to add error cases for API failures
---
